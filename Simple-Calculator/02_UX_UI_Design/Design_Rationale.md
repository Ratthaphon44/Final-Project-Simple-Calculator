# Design Rationale: Scientific Calculator

## 1. การออกแบบเค้าโครง (Layout Design)
### **Grid Layout (5 Columns)**
- **การตัดสินใจ:** เลือกใช้ CSS Grid แบ่งเป็น **5 คอลัมน์** แทนที่จะเป็น 4 คอลัมน์แบบเครื่องคิดเลขทั่วไป
- **เหตุผล:** เนื่องจากระบบเป็น Scientific Calculator ที่มีปุ่มฟังก์ชันเพิ่มเติม (sin, cos, tan, sqrt, ^, parens) การใช้ 5 คอลัมน์ช่วยให้สามารถวางปุ่มฟังก์ชันวิทย์ฯ ไว้ด้านซ้ายสุดประกบกับปุ่มตัวเลขได้พอดี ทำให้ผู้ใช้ไม่ต้องกดปุ่ม Shift หรือเปลี่ยนโหมดไปมา ทุกฟังก์ชันสามารถเข้าถึงได้ในหน้าเดียว (Single View)

### **Responsive Design**
- **การตัดสินใจ:** กำหนดความกว้าง `max-width: 420px` และใช้หน่วย `fr` (fraction) ในการจัดขนาดปุ่ม
- **เหตุผล:** เพื่อให้ Interface ดูสมส่วนทั้งบนหน้าจอ Desktop และ Mobile บนมือถือปุ่มจะมีขนาดใหญ่พอที่จะใช้นิ้วกดได้แม่นยำ (Touch-friendly) ตามหลัก Fitts's Law

## 2. การใช้สีและลำดับความสำคัญ (Color Coding & Visual Hierarchy)
เราใช้สีในการแบ่งกลุ่มฟังก์ชัน (Functional Grouping) เพื่อให้ผู้ใช้แยกแยะปุ่มได้รวดเร็ว:

- **⚪ ปุ่มตัวเลข (Numbers):** ใช้สีขาว (`#ffffff`) หรือสีพื้นฐาน เพื่อให้ดูสะอาดตาและเป็นจุดพักสายตา
- **🟡 ปุ่มเครื่องหมาย (Operators +, -, *, /):** ใช้สีเหลืองอ่อน (`#fff3cd`) เพื่อแยกโซนการกระทำทางคณิตศาสตร์ออกจากตัวเลข
- **🔵 ปุ่มฟังก์ชันวิทยาศาสตร์ (Scientific Ops):** ใช้สีฟ้าอ่อน (`#e1f5fe`) เพื่อบ่งบอกว่าเป็นฟังก์ชันพิเศษที่แตกต่างจาก Basic Operators
- **🔴 ปุ่มลบและเคลียร์ (C, ⌫):** ใช้โทนสีแดงอ่อน (`#ffe6e6`) สื่อถึงการลบหรือการยกเลิก (Destructive Actions)
- **🔵 ปุ่มเท่ากับ (=):** ใช้สีน้ำเงินเข้ม (`#2b7cff`) พร้อมตัวอักษรสีขาว เป็นสีที่มี Contrast สูงที่สุดในหน้าจอ (Primary Action Button) เพื่อดึงดูดสายตาให้ผู้ใช้กดเพื่อดูผลลัพธ์

## 3. การออกแบบส่วนแสดงผล (Display Area)
### **Dual-Line Display**
- **การตัดสินใจ:** แบ่งหน้าจอแสดงผลเป็น 2 ส่วน คือ
    1.  **Expression Line (บน):** แสดงสมการที่กำลังพิมพ์ (เช่น `sin(30)+5`) ตัวเล็กและสีจางกว่า
    2.  **Result Line (ล่าง):** แสดงผลลัพธ์ปัจจุบัน ตัวใหญ่และหนา
- **เหตุผล:** ช่วยแก้ปัญหา "Memory Load" ของผู้ใช้ ผู้ใช้ไม่ต้องจำว่าเมื่อกี้กดอะไรไปแล้วบ้าง และสามารถตรวจสอบความถูกต้องของสมการ (Verification) ก่อนที่จะเชื่อผลลัพธ์ได้

## 4. การตอบสนองต่อผู้ใช้ (Interaction & Feedback)
- **Visual Feedback:** เมื่อมีการเอาเมาส์ไปวาง (Hover) หรือกดปุ่ม (Active) ปุ่มจะมีการขยับลงเล็กน้อย (`transform: translateY(1px)`) และเปลี่ยนเฉดสี เพื่อให้ผู้ใช้รู้สึกถึงการกด (Tactile Feel simulation)
- **Keyboard Accessibility:** รองรับการพิมพ์ผ่านคีย์บอร์ด (Numpad) เพื่อความสะดวกสำหรับผู้ใช้งานบน Desktop ที่ต้องการความรวดเร็วในการกรอกตัวเลข

## 5. การเลือกใช้ฟอนต์ (Typography)
- **การตัดสินใจ:** ใช้ฟอนต์ตระกูล **Inter** หรือ Sans-serif
- **เหตุผล:** เป็นฟอนต์ที่ไม่มีเชิง อ่านง่ายบนหน้าจอแสดงผลดิจิทัล (Legibility) โดยเฉพาะเมื่อต้องแสดงตัวเลขและสัญลักษณ์ทางคณิตศาสตร์ที่มีความซับซ้อน